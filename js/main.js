'use strict';

(function () {
  window.data.getComments(window.data.userText, window.data.userName);
  window.gallery.addFragment();
  window.form.onselectedFile();
  window.picture.onEffectsForm();
  window.input.onSubmitButton();
})();
